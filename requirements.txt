pandas>=2.0.0
numpy>=1.24.0
networkx>=3.1
pyarrow>=12.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
scikit-learn>=1.3.0
xgboost>=2.0.0
reportlab>=4.0.0
google-generativeai>=0.3.0
httpx>=0.27.0
aiokafka>=0.11.0
# Optional — installed only when you also need the windowed-analytics layer:
#   pip install pathway
# Optional — installed only when you want the GraphSAGE / GAT GNN models:
#   pip install torch torch-geometric
